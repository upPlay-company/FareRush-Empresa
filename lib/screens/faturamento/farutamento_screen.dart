import 'package:flutter/material.dart';

class FaturamentoScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Faturamento')),
    );
  }
}
